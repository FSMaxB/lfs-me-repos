#!/bin/bash
pkgname=liblqr
pkgver=0.4.2
pkgver_postfix=

sources=( "http://liblqr.wikidot.com/local--files/en:download-page/${pkgname}-1-${pkgver}.tar.bz2" )
sha1sums=( '69639f7dc56a084f59a3198f3a8d72e4a73ff927' )

lfs_me_prepare() {
    tar -xf "${sources_dir}/${pkgname}-1-${pkgver}.tar.bz2" -C "$build_dir"

    cd "${build_dir}/${pkgname}-1-${pkgver}"

    ./configure --prefix=/usr
}

lfs_me_check() {
    return
}
