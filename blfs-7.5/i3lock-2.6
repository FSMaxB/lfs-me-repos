#!/bin/bash
pkgname=i3lock
pkgver=2.6
pkgver_postfix=

sources=( "http://i3wm.org/i3lock/${pkgname}-${pkgver}.tar.bz2" )
sha1sums=( '7133e00e2c17a48c791fdaf9597a9c6279c51763' )

lfs_me_prepare() {
    tar -xf "${sources_dir}/${pkgname}-${pkgver}.tar.bz2" -C "$build_dir"

    cd "${build_dir}/${pkgname}-${pkgver}"

    sed -i -e 's:login:system-auth:' i3lock.pam
}

lfs_me_check() {
    return
}
