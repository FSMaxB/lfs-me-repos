#!/bin/bash
pkgname=keyutils
pkgver=1.5.8
pkgver_postfix=

sources=( "http://people.redhat.com/~dhowells/${pkgname}/${pkgname}-${pkgver}.tar.bz2" )
sha1sums=( '730af7d23f7d6aa448fa222d9a41990373afc4cc' )

lfs_me_prepare() {
    tar -xf "${sources_dir}/${pkgname}-${pkgver}.tar.bz2" -C "$build_dir"

    cd "${build_dir}/${pkgname}-${pkgver}"
}

lfs_me_check() {
    return
}

lfs_me_install() {
    make DESTDIR="$fakeroot_dir" NO_ARLIB=1 install
}
