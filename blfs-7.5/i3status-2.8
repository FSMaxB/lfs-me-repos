#!/bin/bash
pkgname=i3status
pkgver=2.8
pkgver_postfix=

sources=( "http://i3.zekjur.net/i3status/${pkgname}-${pkgver}.tar.bz2" )
sha1sums=( '34c874c23fef0c5652f4449c6c8e4f8df57bfeb9' )

lfs_me_prepare() {
    tar -xf "${sources_dir}/${pkgname}-${pkgver}.tar.bz2" -C "$build_dir"

    cd "${build_dir}/${pkgname}-${pkgver}"
}

lfs_me_check() {
    return
}

lfs_me_postinstall() {
    setcap 'CAP_NET_ADMIN=ep' /usr/bin/i3status
}
